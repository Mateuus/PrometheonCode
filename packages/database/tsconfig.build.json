{
  // Build publicável: mesmo compilador do `typecheck`, sem os testes no `dist`.
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "tsBuildInfoFile": "./dist/.tsbuildinfo.build"
  },
  "exclude": ["src/**/*.test.ts"]
}
